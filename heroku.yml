build:
  pre:
    - add-apt-repository ppa:ubuntugis/ppa
    - apt update
    - apt install gdal-bin
  languages:
    - java
run:
  web: java -jar target/NorthPine-1.0-SNAPSHOT-jar-with-dependencies.jar